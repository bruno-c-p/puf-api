export const list = (ctx) => {
  ctx.body = 'user list'
}

export const create = (ctx) => {
  ctx.body = 'user list'
}

export const update = (ctx) => {
  ctx.body = 'user list'
}

export const remove = (ctx) => {
  ctx.body = 'user list'
}
